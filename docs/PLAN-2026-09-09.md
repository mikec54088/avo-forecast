# Plan of record — 2026-09-09

Decided by the human on 2026-09-09 after the assessment in this session. This
file exists so a fresh session (any model) can resume without the conversation.
Update the STATUS lines as work lands; do not delete finished steps.

## The decision

Two tracks, A then B. G5 is AUTHORIZED for track B — the human said "Do both."

**Track A — widen the book-only search, on existing data.** Rewrite
`variation_prompt`, then run generation 4 against the ~88k observations on
disk. Free and fast. If it finds nothing, that CLOSES the book-microstructure
space with a clean null: stop generating book-only candidates after that.

**Track B — open `kalshi_research` (G5, authorized).** Forward-only evaluation
via an append-only forecast log. This is the real bet; the book space is
probably empty and the edge, if any, lives in information the book lacks.

## Why (the evidence, so it need not be re-derived)

- 0/39 candidates pass the P&L gate on both selection and confirmation series
  (91,444 obs, 2026-09-08). Generation 1 is DEFINITIVELY refuted: MDE 0.4-0.6c,
  observed -0.5 to -2.7c/contract. Not inconclusive -- refuted.
- The search circles: 7/39 candidates read ONLY `implied_prob`; 12 read only
  midpoint + own price_history. A function of the market's own price can only
  win if that price is miscalibrated as a function of itself (favourite-
  longshot: 1-3c vs 2c cost). A third of all work is in a proven-empty space.
- The prompt steers it there: exemplar `favourite_longshot.py` IS a midpoint
  transform; tried-list sorted by skill (the artifact metric); "UNEXPLORED"
  names 2 fields; "abstain often" without a floor breeds unfalsifiable variants
  (gen 3: 6/8 the same idea, 16-644 fills).
- Learned facts DO reach the agent (CLAUDE.md loads from cwd; 11 candidates
  cite the cost hurdle, 4 carry SPREAD_TOL). What doesn't transfer: WHY each
  family fails. It gets a leaderboard, not mechanisms.
- Unexplored and well-populated: event coherence (`sibling_sum` used by 0/39;
  65.3% of obs have >=2 quoted siblings), book depth (3/39; present on 100%,
  median 92 contracts), time-to-close (2/39; median horizon 0.5h).
  `is_mve` is DEAD: 0 observations. Do not chase it.
- Replay ("backtest") here is a forward test executed by replay: INVARIANT #1
  scores only on outcomes unknown at created_at. Clean for book-only
  candidates. FATAL for research: research runs at scoring time and today's web
  knows last month's outcome. Research must be evaluated forward-only. That is
  MORE rigorous, not less; its only cost is time, and Kalshi's ~5,100
  resolutions/day make a selective candidate measurable in ~2 weeks.
- Structural insight: "evaluation is free so generate wide" confined the search
  to the free-to-evaluate space, which is the empty one. Research inverts the
  loop to few-and-deliberate. That is correct, not a compromise.

## Track A — steps

A1. Rewrite `KalshiQuantExperiment.variation_prompt` in
    `experiments/kalshi_quant/experiment.py`:
    - order the tried-list by P&L verdict (GATE_FAIL first as "PROVEN TO LOSE
      MONEY", then unproven, then any pass), NOT by skill
    - carry the MECHANISM of failure per family, not just numbers:
      midpoint transforms = staleness artifact, skill without money;
      price-banded shoulders = right direction, below the cost hurdle;
      narrow-gate favourites = unfalsifiable, too few fills
    - exemplar: point at a non-midpoint candidate for house style
    - UNEXPLORED: name event coherence (`context.sibling_sum`, `siblings`),
      book depth (`yes_bid_size`/`yes_ask_size`), horizon (`close_time -
      observed_at`), `series_history`; say `is_mve` and `liquidity` are dead
    - abstention FLOOR: must be able to reach 200 fills within ~a week at
      ~5,100 obs/day => act on >= ~1% of markets; state it as a number
    - add the weekend-staleness fact: skill concentrated on weekend sports
      days is the artifact, not a discovery
    STATUS: DONE (commit 080695f)
A2. Tests for the prompt content (tests/test_generation.py or a new
    tests/test_prompt.py): ordering by verdict, presence of mechanisms,
    presence of the floor, exemplar not a midpoint transform.
    STATUS: DONE tests/test_prompt.py
A3. Commit.  STATUS: DONE
A4. Run generation 4.
    STATUS: RAN 2026-09-09, PRODUCED NOTHING. 0/8 accepted, checkpointed as
    gen002 of run kalshi_quant-20260905T044920Z. Seven of eight invocations
    died with "You're out of usage credits. Switch to another model"; the
    eighth wrote no forecast()/MANIFEST. This says NOTHING about the rewritten
    prompt -- it was never exercised. The prompt itself rendered correctly and
    G2 worked: parents were stable_favourite and unchurned_favourite (unproven,
    positive P&L) rather than the skill artifacts.
    RELAUNCHED 2026-09-10 as gen003 with Opus 5 PINNED:
      nohup uv run python -m avo.cli evolve kalshi_quant \
        --run-id kalshi_quant-20260905T044920Z \
        --model claude-opus-5 -n 8 --timeout 1800 \
        > runs/kalshi_quant-20260905T044920Z/gen003-evolve.log 2>&1 &
    The credit exhaustion was FABLE ONLY -- it was the CLI default because the
    human had set it. Sonnet 5 and Opus 5 both work. Always pin --model:
    unpinned follows a user setting that can change under the run, and
    INVARIANT #7 requires the backend be recorded anyway (it is, in
    RunState.backend / GenerationRecord.backend).
    Opus for GENERATING (8 calls a generation, each buying a week of forward
    evaluation to disprove); Sonnet for RESEARCH (retrieval + summarisation,
    cost scales with market count). Smoke-tested before launch.
    STATUS: RUNNING. Check `tail runs/.../gen003-evolve.log` and
    `ls runs/kalshi_quant-20260905T044920Z/` for gen003.json.
A5. Record gen-4 outcome in CLAUDE.md "What has actually been learned".
    If nothing clears the gate: the book space is closed. Say so there.
    STATUS: pending

## Track B — steps (G5 authorized 2026-09-09)

B1. Read docs/EXPERIMENTS.md and experiments/kalshi_quant/experiment.toml to
    see how an experiment is registered; find `kalshi_research` "planned".
    STATUS: DONE
B2. Create experiments/kalshi_research/ with:
    - types.py: reuse kalshi_quant's MarketSnapshot; ForecastContext gains a
      research capability (a callable the candidate may invoke). This is a NEW
      contract for a NEW experiment, not a change to kalshi_quant's (so not G1
      for kalshi_quant).
    - forecast_log.py: append-only Parquet log of
      (candidate_id, ticker, forecast, yes_bid, yes_ask, observed_at, cost).
      The timestamp IS the holdout. created_at is irrelevant here.
    - runner.py: every N minutes, for open markets passing a filter, run each
      live candidate and append. Must be a launchd agent like capture
      (scripts/launchd/); cron is TCC-blocked on this Mac.
    - experiment.py: score() reads the log joined to resolutions instead of
      replaying snapshots. REUSE scoring.py (Brier skill vs market-at-forecast,
      bootstrap_ci_clustered) and paper_trade/passes_pnl_gate/pnl_verdict from
      kalshi_quant unchanged -- they consume (forecast, market, outcome) and do
      not care where it came from. INVARIANTS 2, 4 hold as-is.
    - cost accounting per candidate; selectivity is a first-class constraint.
    STATUS: DONE (revised same day: full-pass source + done-rule, see B5
    notes). Files: experiments/kalshi_research/{types,researcher,
    forecast_log,runner,experiment}.py, candidates/research_market.py,
    tests/test_research.py (12 tests), scripts/launchd/
    com.avoforecast.kalshi-research-run.plist. `avo rank kalshi_research`
    works once forecasts resolve (Experiment.scoring_inputs() added to the
    protocol; kalshi_quant got one too).
B3. Promote kalshi_research from status="planned" (the G5 act itself).
    STATUS: DONE (experiment.toml status="active")
B4. Start the runner with ZERO candidates if needed, but get the log + launchd
    running first. The clock starts when the log starts.
    STATUS: DONE 2026-09-09 15:15 UTC. First pass: 82 markets, 0 research
    calls. launchd agent com.avoforecast.kalshi-research-run loaded, runs at
    :05/:20/:35/:50, output in data/research.log. Only the control runs, so
    it spends nothing. Verify: `launchctl list | grep research` exit 0 and
    `tail data/research.log` shows a line per pass.
B5. One hand-written research candidate to prove the path end to end.
    STATUS: DEPLOYED 2026-09-10 at 1-in-4 (human's instruction).
    experiments/kalshi_research/candidates/injury_news_favourite.py, validated
    by the probe in place, picked up by the hourly launchd runner. Verified
    against the live full pass: it wants to research 4 markets per pass.

    IT IS CURRENTLY DOING NOTHING, BY DESIGN. The Claude CLI is out of usage
    credits (verified 2026-09-10 by a live call: exit 1, body "You're out of
    usage credits"). Every research call fails, so every one of its markets is
    DEFERRED and retried, and the runner prints a WARNING line per pass. The
    moment credits return it starts working with no intervention.

    Three guards added the same day, because the failure was silent otherwise:
    the credit message contains none of the candidate's keywords, so it reads
    as "no news found" and the candidate abstains, looking exactly like the
    control.
      - ResearchContext.research_failed distinguishes a broken channel from a
        genuine "nothing found".
      - The runner DEFERS a market whose research failed instead of consuming
        it; inside --final-hours it logs it with research_error recorded.
      - score() EXCLUDES rows carrying a research_error: such a row holds the
        market's own number, not a forecast.
    Check: `tail data/research.log` -- "N research calls (N FAILED)" plus a
    WARNING means credits are still out; "(0 FAILED)" means it is live.

## How to verify## How to verify## How to verify you are resuming correctly

    git log --oneline -8        # last commit should be at or after f5e3c16
    grep -n STATUS docs/PLAN-2026-09-09.md
    uv run pytest -q            # 236+ passing
    launchctl list | grep avoforecast   # capture: all exit 0, standing priority

Memory index entry: ~/.claude/projects/-Users-hermesagent-development-avo-forecast/memory/plan-2026-09-09.md
