# Phase 0. Install with: crontab -e — adjust paths.
*/15 * * * * cd /path/to/avo-forecast && .venv/bin/python -m experiments.kalshi_quant.capture snapshot >> data/capture.log 2>&1
17   * * * * cd /path/to/avo-forecast && .venv/bin/python -m experiments.kalshi_quant.capture settle   >> data/capture.log 2>&1
